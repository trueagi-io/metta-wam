**Tests which test for MeTTa compatibility**.

All tests are executed when ```sh test.sh``` is executed in the project's main directory, whereby any output mismatch with the MeTTa interpreter is reported!
