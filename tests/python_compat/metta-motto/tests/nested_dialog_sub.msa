; A dumb agent, which expects the system message and a user message
; and simply responds with the user message back
(= (response)
   (let (Messages (system "Answer the user") (user $x)) (messages) $x))
