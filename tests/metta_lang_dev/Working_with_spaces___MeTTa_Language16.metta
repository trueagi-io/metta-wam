
; 20241014 r.v.v converted metta-lang.dev to metta 
;  !(assertEqualToResult ( )  (  )  ) 
! (get-atoms &amp;self)! (match &amp;self
          (= (if $cond $then $else) $result)
   (quote (= (if $cond $then $else) $result))
  )

;  !(assertEqualToResult ( )  (  )  ) 
